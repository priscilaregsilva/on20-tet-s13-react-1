function LearningCss(props) {

    return(
        <div>
            <h1>CSS</h1>
            CSS - responsável por estilizar, deixar a informação, a página bonita visualmente.
            CSS (Cascading Style Sheets ou Folhas de Estilo em Cascata) é uma linguagem de estilo,
            usada para descrever a apresentação de um documento escrito em HTML.
            <br />
            <img src={props.gifcss} />
        </div>

    )
}

export default LearningCss